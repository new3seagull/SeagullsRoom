spring:
  profiles:
    default: local

  datasource:
    url: jdbc:h2:mem:~/SeagullsRoom
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none


  jwt:
    secret: vmfhaltmskdlstkfkdgodyroqkfwkdbalroqkfwkdbalaaaaaaaaaaaaaaaabbbbb
openai:
  model: gpt-4o
  api:
    key: sk-proj-SktJ6kuNSDEbUdQNaRA6T3BlbkFJy01JQdc59Lxx1HNWelQ0
    url: https://api.openai.com/v1/chat/completions

---

spring:
  config:
    activate:
      on-profile: local

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true

  h2:
    console:
      enabled: true

---

spring:
  config:
    activate:
      on-profile: test

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never # init sql 실행 x


